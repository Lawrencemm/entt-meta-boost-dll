# entt::meta_any across dynamically loaded shared libraries

## Setup
Requires Conan package manager.

Run `mkdir build && conan install . -if build` to get dependencies. Run CMake
in that build folder.
